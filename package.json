{
  "name": "root",
  "version": "1.0.0",
  "description": "一个可以托拽的vue3+typescript组件，主要应用场景：两个子空间的拖拽状态的相互转换",
  "scripts": {
    "build": "pnpm run --filter a-draggable-component build",
    "watch": "pnpm run --filter a-draggable-component watch",
    "dev:docs": "pnpm run --filter site dev:docs",
    "dev": "pnpm run --filter site dev",
    "build:docs": "pnpm run --filter site build:docs",
    "serve:docs": "pnpm run --filter site serve:docs",
    "pub": "pnpm build && pnpm publish --filter a-draggable-component",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "vue3",
    "ts",
    "draggable"
  ],
  "author": "xinhong.wang",
  "license": "ISC",
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.5.0",
    "less": "^4.2.0",
    "typescript": "^5.2.2",
    "vite": "^5.0.0",
    "vue": "^3.2.36"
  }
}

